name: room_312
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  # Główne biblioteki analityczne i GUI (Conda)
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - statsmodels
  - pyside6
  - requests
  - pydantic
  - scipy
  # Sekcja PIP (biblioteki handlowe i pandas_ta)
  - pip:
      - pandas_ta
      - ccxt
      - backtesting
      - pybit
      - cryptography
      - pyyaml
      - tqdm
      - websocket-client
      - bokeh